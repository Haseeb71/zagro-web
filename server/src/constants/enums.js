const USER_TYPES = {
    ADMIN: "admin",
    CUSTOMER: 'customer',
    WORKER: 'worker'
}

const FACTORY_PRODUCT_TYPE = {
    RAW_MATERIAL: "raw",
    WAREHOUSE: "warehouse",
    MACHINERY: "machinery"
}

module.exports = { USER_TYPES, FACTORY_PRODUCT_TYPE };